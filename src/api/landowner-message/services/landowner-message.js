'use strict';

/**
 * landowner-message service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::landowner-message.landowner-message');
